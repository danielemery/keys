name: Publish

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  docker-publish:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: nowsprinting/check-version-format-action@v5.0.1
        id: version
        with:
          prefix: "v"

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  helm-publish:
    needs:
      - docker-publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: nowsprinting/check-version-format-action@v5.0.1
        id: version
        with:
          prefix: "v"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.HELM_DEPLOY_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.HELM_DEPLOY_SECRET }}
          aws-region: ${{ vars.HELM_DEPLOY_REGION }}

      # Replace the values.yaml version field with the new version
      - name: Update image version in the values.yaml
        run: |
          sed -i "s/REPLACED_BY_CI/${{ steps.version.outputs.full }}/g" helm/values.yaml

      - name: Publish chart
        uses: danielemery/helm-release-action@f19adb815088a067bb839b224decb0611072652d
        with:
          repo: s3://helm.demery.net/
          chart: ./helm
          version: ${{ steps.version.outputs.full }}
          appVersion: ${{ steps.version.outputs.full }}

  rust-cli-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: nowsprinting/check-version-format-action@v5.0.1
        id: version
        with:
          prefix: "v"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@67ef31d5b988238dd797d409d6f9574278e20537 # master
        with:
          toolchain: 1.89.0

      # Stamp the released binary with the tag version. Cargo.toml ships a
      # 0.0.0 placeholder, so clap's `--version` would otherwise report 0.0.0.
      - name: Set CLI version from tag
        working-directory: ./cli
        run: sed -i "s/^version = .*/version = \"${{ steps.version.outputs.full }}\"/" Cargo.toml

      - name: Build Rust CLI
        working-directory: ./cli
        run: cargo build --release

      - name: Upload Rust CLI binary
        uses: actions/upload-artifact@v4
        with:
          name: keys-cli-${{ steps.version.outputs.full }}
          path: cli/target/release/keys
          retention-days: 30

  github-release:
    needs:
      - docker-publish
      - helm-publish
      - rust-cli-build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: nowsprinting/check-version-format-action@v5.0.1
        id: version
        with:
          prefix: "v"

      - name: Download Rust CLI binary
        uses: actions/download-artifact@v4
        with:
          name: keys-cli-${{ steps.version.outputs.full }}
          path: ./artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.full }}
          name: Release ${{ steps.version.outputs.full }}
          generate_release_notes: true
          files: |
            ./artifacts/keys
